import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 8, 186, 221);
const secondaryColor = Color.fromARGB(255, 43, 43, 56);
const darkColor = Color(0xFF191923);
const bodyTextColor = Color(0xffCEC7BF);
// Color(0xFF8B8B8D);
const bgColor = Color(0xFF1E1E28);
const cream = Color(0xffCEC7BF);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web
